name: 01 - Building Blocks

on: workflow_dispatch

jobs:
    job-1:
        runs-on: ubuntu-latest
        steps:
            - name: job-1 step-1
              run: |
                echo "hello, world! from job-1 step-1 "
                hostname

    job-2:
        runs-on: ubuntu-latest
        steps:
            - name: job-2 step-1
              run: |
                hostname
 
